import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
    content: {
        padding: 16,
        flex: 1,
    },
    paragraph: {
        fontSize: 24,
        marginBottom: 16,
    },
});

export default styles;
